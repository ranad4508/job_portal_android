<resources>

    <!-- Define your custom tab style -->
    <style name="CustomTab">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:padding">16dp</item>
        <item name="android:background">?attr/selectableItemBackground</item> <!-- Optional: Adds ripple effect -->
    </style>
</resources>
